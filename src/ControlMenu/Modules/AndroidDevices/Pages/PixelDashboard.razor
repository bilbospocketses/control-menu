@page "/android/phone"
@page "/android/phone/{DeviceId:guid}"
@page "/android/pixel"
@page "/android/pixel/{DeviceId:guid}"
@using ControlMenu.Data.Enums

<DeviceDashboard Title="Android Phone" Icon="bi-phone" DeviceType="DeviceType.AndroidPhone"
                 DeviceKind="phone" DeviceId="@DeviceId" />

@code {
    [Parameter] public Guid? DeviceId { get; set; }
}
