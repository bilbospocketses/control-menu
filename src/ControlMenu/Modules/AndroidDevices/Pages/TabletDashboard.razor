@page "/android/tablet"
@page "/android/tablet/{DeviceId:guid}"
@using ControlMenu.Data.Enums

<DeviceDashboard Title="Android Tablet" Icon="bi-tablet-landscape" DeviceType="DeviceType.AndroidTablet"
                 DeviceKind="tablet" DeviceId="@DeviceId" />

@code {
    [Parameter] public Guid? DeviceId { get; set; }
}
