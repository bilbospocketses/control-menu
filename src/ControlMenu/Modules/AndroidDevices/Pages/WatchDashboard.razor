@page "/android/watch"
@page "/android/watch/{DeviceId:guid}"
@using ControlMenu.Data.Enums

<DeviceDashboard Title="Android Watch" Icon="bi-smartwatch" DeviceType="DeviceType.AndroidWatch"
                 DeviceKind="watch" DeviceId="@DeviceId" />

@code {
    [Parameter] public Guid? DeviceId { get; set; }
}
